<h1><%= @user.username %>'s Photos </h1>

<% @user.photos.each do |photo| %>
  <p><%= photo.description %></p>
  <p><%= image_tag photo.picture.url %></p>
  <p><%= image_tag photo.picture.url(:medium) %></p>
  <p><%= image_tag photo.picture.url(:thumb) %></p>
<% end %>
